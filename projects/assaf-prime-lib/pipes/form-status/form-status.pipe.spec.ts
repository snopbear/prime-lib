import { FormStatusPipe } from './form-status.pipe';

describe('FormStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FormStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
